<Window
	x:Class="RevToGOSTv0.MainWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:dd="urn:gong-wpf-dragdrop"
	xmlns:local="clr-namespace:RevToGOSTv0"
	mc:Ignorable="d" 
	Title="MainWindow" Height="500" Width="800"
	ResizeMode="CanMinimize" 
	WindowStartupLocation="CenterOwner">

	<Grid>

		<TabControl
			TabStripPlacement="Left">
			<TabItem
				Header="2. Выбор категорий" Height="24" VerticalAlignment="Top">
				<Grid>

					<TextBlock
						Text="Доступные категории"
						VerticalAlignment="Center"
						Margin="61,14,353,426" Height="25"/>
					<ListView
						x:Name="AvailableCategories"
						Margin="61,44,353,22"
						ItemsSource="{Binding}"
						SelectionMode="Extended"
						dd:DragDrop.IsDragSource="True"
						dd:DragDrop.IsDropTarget="True">
						<ListView.View>
							<GridView>
								<GridViewColumn DisplayMemberBinding="{Binding Path=Name}" Header="Категория"/>
								<GridViewColumn DisplayMemberBinding="{Binding Path=Count}" Header="Элем."/>
							</GridView>
						</ListView.View>
					</ListView>

					<TextBlock
						Text="Выбранные категории"
						VerticalAlignment="Center" 
						Margin="361,14,53,426" Height="25"/>
					<ListView
						x:Name="PickedCategories"
						Margin="361,44,53,22"
						ItemsSource="{Binding}"
						SelectionMode="Extended"
						dd:DragDrop.IsDragSource="True"
						dd:DragDrop.IsDropTarget="True">
						<ListView.View>
							<GridView>
								<GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Категория"/>
								<GridViewColumn DisplayMemberBinding="{Binding Count}" Header="Элем."/>
							</GridView>
						</ListView.View>
					</ListView>

				</Grid>
			</TabItem>

			<TabItem Header="3. Выбор элементов" >
				<Grid>

					<TextBlock
						Text="Доступные элементы"
						VerticalAlignment="Center"
						Margin="61,14,353,426" Height="25"/>
					<ListView
						x:Name="AvailableElements"
						Margin="61,44,353,22"
						ItemsSource="{Binding}"
						SelectionMode="Extended"
						dd:DragDrop.IsDragSource="True"
						dd:DragDrop.IsDropTarget="True">
						<ListView.View>
							<GridView>
								<GridViewColumn DisplayMemberBinding="{Binding Path=Name }" Header="Категория"/>
								<GridViewColumn DisplayMemberBinding="{Binding Path=Count}" Header="Элем."/>
							</GridView>
						</ListView.View>
					</ListView>

					<TextBlock
						Text="Выбрано"
						VerticalAlignment="Center" 
						Margin="361,14,53,426" Height="25"/>
					<ListView
						x:Name="PickedElements"
						Margin="361,44,53,22"
						ItemsSource="{Binding}"
						SelectionMode="Extended"
						dd:DragDrop.IsDragSource="True"
						dd:DragDrop.IsDropTarget="True">
						<ListView.View>
							<GridView>
								<GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Категория"/>
								<GridViewColumn DisplayMemberBinding="{Binding Count}" Header="Элем."/>
							</GridView>
						</ListView.View>
						<ListView.GroupStyle>
							<GroupStyle>
								<GroupStyle.HeaderTemplate>
									<DataTemplate>
										<TextBlock FontWeight="Bold" FontSize="14" Text="{Binding Name}"/>
									</DataTemplate>
								</GroupStyle.HeaderTemplate>
							</GroupStyle>
						</ListView.GroupStyle>
					</ListView>

					<Button x:Name="btn" Click="btn_Click" Content="Button" Margin="630,287,16,22"/>

				</Grid>
			</TabItem>

			<TabItem Header="4. Доп. параметры"/>
		</TabControl>

	</Grid>

	<Window.Resources>
	</Window.Resources>

</Window>
